[package]
name = "vfio_sys"
version = "0.1.0"
authors = ["The ChromiumOS Authors"]
edition = "2021"
license = "BSD-3-Clause"

[dependencies]
base = { workspace = true }
zerocopy = { version = "0.8.13", features = ["derive"] }
