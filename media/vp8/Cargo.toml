[package]
name = "vp8"
version = "0.1.0"
authors = ["The Chromium OS Authors"]
edition = "2021"

[dependencies]
anyhow = "*"
bytes = "1.1.0"
log = { version = "0", features = ["release_max_level_debug"]}
