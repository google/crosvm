[package]
name = "sys_util_core"
version = "0.1.0"
authors = ["The Chromium OS Authors"]
edition = "2021"
include = ["src/**/*", "Cargo.toml"]

[dependencies]
libc = "*"
remain = "0.2"
serde = { version = "1", features = [ "derive" ] }
thiserror = "*"

[workspace]
members = [
    "poll_token_derive",
]
