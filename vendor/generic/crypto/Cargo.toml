[package]
name = "crypto_generic"
version = "0.1.0"
authors = ["The ChromiumOS Authors"]
edition = "2021"

[features]
rustcrypto = []

[dependencies]
anyhow = { workspace = true }
serde = { workspace = true, features = ["derive"] }
zeroize = "1.5.7"

[dev-dependencies]
tempfile = "3"
