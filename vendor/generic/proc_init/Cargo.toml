[package]
name = "proc_init_product"
authors = ["The ChromiumOS Authors"]
version = "0.1.0"
edition = "2021"
license = "BSD-3-Clause"

[features]
process-invariants = []
crash-report = ["crash_report"]

[dependencies]
anyhow = { workspace = true }
crash_report = { workspace = true, optional = true }
serde = { workspace = true, features = ["derive"] }
