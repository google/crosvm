[package]
name = "integration_tests"
version = "0.1.0"
authors = ["The Chromium OS Authors"]
edition = "2021"

[dev-dependencies]
tempfile = "3"
crosvm = { path = ".." }
arch = { path = "../arch" }
base = "*"
libc = "0.2.65"
anyhow = "*"
