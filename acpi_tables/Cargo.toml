[package]
name = "acpi_tables"
version = "0.1.0"
authors = ["The ChromiumOS Authors"]
edition = "2021"
license = "BSD-3-Clause"

[dependencies]
zerocopy = { version = "0.8.13", features = ["derive"] }

[dev-dependencies]
tempfile = "3"
